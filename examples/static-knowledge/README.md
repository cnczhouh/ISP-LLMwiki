# Static Knowledge Example

这是一个用于 GitHub Pages / 静态部署演示的最小知识库。它只包含脱敏示例，不依赖本地绝对路径，也不包含真实 raw PDF。

可用于验证：

- Markdown 页面生成
- 页面属性读取
- Obsidian 风格 `[[双向链接]]`
- 来源小节
- 本地搜索和知识库浏览

运行示例构建：

```bash
npm run build:static:example
```

